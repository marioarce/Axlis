name: CI

on:
  push:
    branches:
      - main
      - develop
      - 'feature/**'
  pull_request:
    branches:
      - main
      - develop

jobs:
  build:
    name: Build & Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET 8
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Restore
        run: dotnet restore Axlis.ORM.sln

      - name: Build
        run: dotnet build Axlis.ORM.sln --no-restore --configuration Release

      - name: Test
        run: dotnet test Axlis.ORM.sln --no-build --configuration Release --collect:"XPlat Code Coverage" --results-directory ./coverage

      - name: Upload coverage artifacts
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: coverage
          path: ./coverage

  build-customizations:
    name: Build & Test (Customizations)
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET 8
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Restore
        run: dotnet restore Axlis.Customizations.sln

      - name: Build
        run: dotnet build Axlis.Customizations.sln --no-restore --configuration Release

      - name: Test
        run: dotnet test Axlis.Customizations.sln --no-build --configuration Release --verbosity normal

  build-sitecore-context:
    name: Build & Test (Sitecore Context)
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET 8
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Restore
        run: dotnet restore Axlis.Sitecore.Context.sln

      - name: Build
        run: dotnet build Axlis.Sitecore.Context.sln --no-restore --configuration Release

      - name: Test
        run: dotnet test Axlis.Sitecore.Context.sln --no-build --configuration Release --verbosity normal
